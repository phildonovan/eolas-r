% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{eolas_get_pharmac}
\alias{eolas_get_pharmac}
\title{Fetch a PHARMAC dataset (NZ pharmaceutical subsidy schedule + hospital medicines)}
\usage{
eolas_get_pharmac(
  name,
  start = NULL,
  end = NULL,
  limit = NULL,
  as_sf = NULL,
  as_arrow = FALSE,
  ...
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_cpi"}.}

\item{start}{ISO date lower bound, e.g. \code{"2020-01-01"}. Optional.}

\item{end}{ISO date upper bound, e.g. \code{"2024-12-31"}. Optional.}

\item{limit}{Max rows to return. Default \code{NULL} requests the full dataset
(server enforces a 50,000-row cap on Free/Starter plans; Pro is unlimited).
Pass an integer to request fewer rows.}

\item{as_sf}{Convert geospatial datasets to an \code{sf} object (CRS = WGS84).
\code{NULL} (default) auto-converts when the dataset has a \code{geometry_wkt}
column AND the \code{sf} package is installed. \code{TRUE} forces conversion (errors
if \code{sf} is missing). \code{FALSE} keeps the raw WKT string column.
Install with \code{install.packages("sf")}. Cannot be combined with
\code{as_arrow = TRUE}.}

\item{as_arrow}{When \code{TRUE}, return an \code{arrow::Table} instead of a
data frame or \code{sf} object (zero-copy, no geometry materialisation).
Cannot be combined with \code{as_sf = TRUE}.}
}
\value{
A \code{eolas_dataset} data frame.
}
\description{
A named wrapper over \code{\link[=eolas_get]{eolas_get()}} for datasets from PHARMAC (Pharmaceutical
Management Agency). Covers the monthly Pharmaceutical Schedule (community-
funded medicines and subsidies) and the Hospital Medicines List (HML),
including full longitudinal archives from 2006 and 2011 respectively.
}
\details{
Historical archive datasets (\code{pharmac_schedule_history},
\code{pharmac_hml_history}) are append-mode; each month's snapshot is tagged
with a \code{time_frame} column (YYYY-MM format).
Source: \url{https://schedule.pharmac.govt.nz/}.
Licence: CC-BY 3.0 NZ (Crown).
}
\examples{
\dontrun{
eolas_key("your_key")
df <- eolas_get_pharmac("pharmac_schedule")          # current month's funded medicines
df <- eolas_get_pharmac("pharmac_schedule_history")  # 2006-present subsidy archive
df <- eolas_get_pharmac("pharmac_hospital_medicines_list")  # current HML
df <- eolas_get_pharmac("pharmac_hml_history")       # 2011-present HML archive
}
}

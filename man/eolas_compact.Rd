% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compact.R
\name{eolas_compact}
\alias{eolas_compact}
\title{Compact a synced dataset directory by merging all parquet files into one}
\usage{
eolas_compact(dataset_dir = NULL, library_dir = NULL, dataset = NULL)
}
\arguments{
\item{dataset_dir}{Path to a specific dataset directory
(e.g. \verb{/data/eolas/nz_parcels}).  When set, \code{library_dir} and
\code{dataset} are ignored.}

\item{library_dir}{Root library directory.  When \code{dataset} is set, compact
only that one dataset; when \code{dataset = NULL} compact all synced datasets
under the library root.}

\item{dataset}{Dataset name to compact within \code{library_dir}.  \code{NULL}
(default) compacts all datasets.}
}
\value{
An \code{eolas_compact_result} S3 object (single dataset), or a named
list of \code{eolas_compact_result} objects when compacting all datasets.
}
\description{
Reads all snapshot and delta parquet files in the dataset directory via
\code{arrow::open_dataset()}, merges them into a single snapshot file, updates
the manifest, and deletes the now-superseded files.
}
\details{
The merge is a pure concatenation — SCD2 tables that carry \code{is_current} /
\code{valid_to} columns are \strong{not} collapsed.  The compacted file contains all
historical rows; readers that want current-state filter by
\code{is_current == TRUE} as usual.
}
\section{Atomicity}{

A crashed compact at any step leaves either the original state intact or a
clearly-named \verb{.compacting-done-<uuid>} directory.  The next compact run
detects and removes stale staging directories automatically.
}

\section{One dataset vs all}{

Pass a path directly to \code{dataset_dir} to compact one dataset, or pass
\code{library_dir} (optionally with \code{dataset}) to compact one or all synced
datasets under a library root.
}

\examples{
\dontrun{
# Compact a specific dataset directory
r <- eolas_compact("/data/eolas/nz_parcels")
print(r)
# <eolas_compact_result dataset=nz_parcels files=5->1 rows=5431319 ...>

# Compact one dataset by name under a library root
r <- eolas_compact(library_dir = "/data/eolas", dataset = "nz_parcels")

# Compact all synced datasets under a library root
results <- eolas_compact(library_dir = "/data/eolas")
}
}

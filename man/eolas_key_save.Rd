% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{eolas_key_save}
\alias{eolas_key_save}
\title{Save your eolas API key to the OS keyring}
\usage{
eolas_key_save(key = NULL)
}
\arguments{
\item{key}{The API key to save. \code{NULL} (default) prompts interactively via
\code{\link[askpass:askpass]{askpass::askpass()}} (if available) or \code{\link[=readline]{readline()}}.}
}
\value{
Invisibly \code{NULL}.
}
\description{
Stores the key in the OS-native credential store (macOS Keychain, Windows
Credential Manager, Linux Secret Service) under
\code{service = "eolas"}, \code{username = "api-key"}. Once saved, \code{\link[=eolas_key]{eolas_key()}} and
every \verb{eolas_get_*()} call will find the key automatically — no environment
variable or explicit call needed in future sessions.
}
\details{
The same keyring slot is read by the Python \code{eolas-data} client, so a key
saved from R is immediately available in Python and vice versa.

Requires the \code{keyring} package. On Linux, \code{libsecret-1-dev} system headers
are needed before \code{install.packages("keyring")}.
}
\examples{
\dontrun{
eolas_key_save()          # interactive prompt
eolas_key_save("vs_...")  # non-interactive
}
}
\seealso{
\code{\link[=eolas_key_clear]{eolas_key_clear()}}, \code{\link[=eolas_key_status]{eolas_key_status()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{eolas_get_lris}
\alias{eolas_get_lris}
\title{Fetch a Manaaki Whenua / LRIS dataset (land cover, soil, protected areas)}
\usage{
eolas_get_lris(
  name,
  start = NULL,
  end = NULL,
  limit = NULL,
  as_sf = NULL,
  as_arrow = FALSE
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_cpi"}.}

\item{start}{ISO date lower bound, e.g. \code{"2020-01-01"}. Optional.}

\item{end}{ISO date upper bound, e.g. \code{"2024-12-31"}. Optional.}

\item{limit}{Max rows to return. Default \code{NULL} requests the full dataset
(server enforces a 50,000-row cap on Free/Starter plans; Pro is unlimited).
Pass an integer to request fewer rows.}

\item{as_sf}{Convert geospatial datasets to an \code{sf} object (CRS = WGS84).
\code{NULL} (default) auto-converts when the dataset has a \code{geometry_wkt}
column AND the \code{sf} package is installed. \code{TRUE} forces conversion (errors
if \code{sf} is missing). \code{FALSE} keeps the raw WKT string column.
Install with \code{install.packages("sf")}. Cannot be combined with
\code{as_arrow = TRUE}.}

\item{as_arrow}{When \code{TRUE}, return an \code{arrow::Table} instead of a
data frame or \code{sf} object (zero-copy, no geometry materialisation).
Cannot be combined with \code{as_sf = TRUE}.}
}
\value{
A \code{eolas_dataset} data frame, or an \code{sf} object when geometry is
present and conversion is enabled.
}
\description{
A named wrapper over \code{\link[=eolas_get]{eolas_get()}} for datasets sourced from the Land
Resource Information System (LRIS), managed by Manaaki Whenua - Landcare
Research NZ. Covers LCDB land-cover vintages (v3.0 through v6), NZLUM land
use management, PBC, and the PAN-NZ protected areas layer.
}
\details{
LCDB v3.0-v4.1 are deprecated vintages retained for longitudinal analysis.
LCDB v5 is superseded by v6 but still served.
\code{pan_nz_2025_draft} was marked Draft at the time of ingestion (2026-05-12).
Source: \url{https://lris.scinfo.org.nz}.
Licence: CC-BY 4.0 International (LCDB v5/v6, NZLUM, PBC, PAN-NZ);
CC-BY 3.0 NZ (LCDB v3/v4 vintages). Attribution: Manaaki Whenua.
}
\examples{
\dontrun{
eolas_key("your_key")
gdf <- eolas_get_lris("lcdb_v6_mainland")   # current NZ land cover
gdf <- eolas_get_lris("nzlum_v03")          # NZ Land Use Management v0.3
gdf <- eolas_get_lris("pan_nz_2025_draft")  # protected areas (Draft, 2025)
}
}

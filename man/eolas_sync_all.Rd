% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{eolas_sync_all}
\alias{eolas_sync_all}
\title{Sync multiple datasets to a local library directory}
\usage{
eolas_sync_all(
  library_dir = NULL,
  datasets = NULL,
  max_concurrent = 4L,
  progress = NULL,
  base_url = EOLAS_BASE_URL
)
}
\arguments{
\item{library_dir}{Root directory of the local data library.  When \code{NULL}
the library is resolved via \code{eolas_resolve_library_dir()}.}

\item{datasets}{Character vector of dataset names to sync.  When \code{NULL}
(default) all sub-directories with a \verb{_eolas-manifest.json} are synced.}

\item{max_concurrent}{Maximum parallel sync operations (default \code{4}).
Ignored on Windows (see Parallelism section).}

\item{progress}{Passed to each \code{\link[=eolas_sync]{eolas_sync()}} call.}

\item{base_url}{Override the API base URL (useful for testing).}
}
\value{
A named list of \code{eolas_sync_result} objects, one per dataset.
On a per-dataset failure the corresponding entry has \code{status = "error"}
and \code{error} set to the error message; other datasets still complete
normally.
}
\description{
Calls \code{\link[=eolas_sync]{eolas_sync()}} for each dataset, optionally in parallel.  When
\code{datasets = NULL} all sub-directories that contain a \verb{_eolas-manifest.json}
are discovered and synced automatically.
}
\section{Parallelism}{

Uses \code{parallel::mclapply()} on Unix-like systems (fork-based, up to
\code{max_concurrent} cores).  \strong{On Windows}, \code{mclapply()} is not supported;
the function falls back to sequential \code{lapply()} automatically with a
one-time message.  Windows pipeline users should wrap individual
\code{eolas_sync()} calls in a \code{future} or background process if concurrency is
needed.
}

\examples{
\dontrun{
eolas_key("your_key")

# Sync specific datasets
results <- eolas_sync_all(
  library_dir = "/data/eolas",
  datasets    = c("doc_huts", "nz_cpi")
)

# Auto-discover all synced datasets and refresh them
results <- eolas_sync_all(library_dir = "/data/eolas")

# Check results
for (r in results) print(r)
}
}

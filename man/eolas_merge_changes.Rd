% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdc.R
\name{eolas_merge_changes}
\alias{eolas_merge_changes}
\title{Merge a batch of change rows into the current local materialised snapshot}
\usage{
eolas_merge_changes(
  local_df,
  changes_df,
  pk_columns,
  current_state_filter = NULL
)
}
\arguments{
\item{local_df}{Current local materialised state (may be a 0-row frame with the right columns).}

\item{changes_df}{Change rows from the \verb{/changes} feed. Must contain \verb{_eolas_seq} and \verb{_eolas_op}.}

\item{pk_columns}{Character vector of primary-key columns (non-empty). The merge keys on these
only — never on geometry.}

\item{current_state_filter}{Optional \code{"<col> = <value>"} filter applied after the merge.}
}
\value{
The merged current-state data frame, meta columns stripped, row names reset.
}
\description{
Faithful port of the Python client's \code{merge_changes}. Drops local rows for every pk the feed
touched (including deletes), appends the non-delete change rows, applies the
\code{current_state_filter}, and strips the \verb{_eolas_*} meta columns. The result is the current state
for the touched pks merged with the untouched local rows.
}

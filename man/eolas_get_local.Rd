% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bulk.R
\name{eolas_get_local}
\alias{eolas_get_local}
\title{Download (or serve from cache) a whole dataset as a local data frame}
\usage{
eolas_get_local(
  name,
  cache_dir = NULL,
  format = NULL,
  freshness = "auto",
  as_sf = NULL,
  as_arrow = FALSE,
  progress = NULL,
  base_url = EOLAS_BASE_URL,
  ...
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_parcels"}.}

\item{cache_dir}{Local directory for cached files.  Accepts \code{~}-prefixed
paths.  Created if it does not exist.  \code{NULL} (default) resolves via the
library precedence chain: \code{EOLAS_LIBRARY} env var,
then \code{library_dir} in \verb{~/.eolas/config.json},
then \verb{~/.cache/eolas/} fallback.  An explicit value here always wins
(highest priority). Use \code{eolas_library_set()} to configure a persistent
location.}

\item{format}{\code{"parquet"}, \code{"csv_gz"}, or \code{"geoparquet"}.  \code{NULL} (default)
auto-detects from dataset metadata.}

\item{freshness}{\code{"auto"} (default), \code{"monthly"}, or \code{"current"}.  Passed
verbatim to \code{\link[=eolas_sync_bulk]{eolas_sync_bulk()}}.}

\item{as_sf}{When \code{TRUE} and the file is GeoParquet, the function
attempts to return an \code{sf} object via \code{sf::st_read()} or
\code{sfarrow::st_read_parquet()}.  When \code{FALSE}, a plain data frame is
returned regardless of geometry.  \code{NULL} (default) is treated as \code{TRUE}
unless \code{as_arrow = TRUE}, in which case it is treated as \code{FALSE}.
Cannot be combined with \code{as_arrow = TRUE} (stops with an error).}

\item{as_arrow}{When \code{TRUE}, skip all native geometry materialisation and
return an \code{arrow::Table} directly.  Geometry stays as Arrow buffers
(zero-copy) — suitable for DuckDB / dplyr pipelines that work on a
sample before converting to sf.  Works for geo and non-geo datasets.
Cannot be combined with \code{as_sf = TRUE} (stops with an error).  Requires
the \code{arrow} package: \code{install.packages("arrow")}.}

\item{progress}{Control the download progress bar. \code{NULL} (default)
auto-detects: bar shown in interactive sessions (\code{interactive()} is
\code{TRUE}), hidden in batch/CI/pipes. \code{TRUE} forces the bar on; \code{FALSE}
forces it off. Also suppressed by \code{EOLAS_NO_PROGRESS=1} in the
environment. When the dataset is already up to date no bar is shown
(no data is transferred).}

\item{base_url}{Override the API base URL (useful for testing).}

\item{...}{Reserved for future arguments; currently ignored.}
}
\value{
A \code{data.frame}, \code{sf} object, or \code{arrow::Table}, depending on the
dataset and the \code{as_sf} / \code{as_arrow} arguments.
}
\description{
This is the recommended path for large or geospatial datasets in an
interactive R session or R Markdown notebook.  On the first call it fetches
the bulk file from CDN (milliseconds for monthly snapshots) and writes it to
\verb{~/.cache/eolas/}.  On subsequent calls a lightweight HEAD request checks
whether the local file is still current; if so the cached copy is read
directly — zero network I/O on the data payload.
}
\details{
If you have been calling \code{eolas_get("nz_parcels")} on a 3-million-row
geospatial dataset and it takes 15+ minutes, use \code{eolas_get_local()}
instead — it serves a pre-materialised GeoParquet from CDN, not a live
Iceberg scan through the row-oriented data endpoint.
}
\section{Format auto-detection}{

When \code{format = NULL} (the default), \code{eolas_get_local()} calls
\code{eolas_info(name)} and checks the metadata for a \code{geometry_type} field.
Geo datasets use \code{"geoparquet"}; everything else uses \code{"parquet"}.
}

\section{GeoParquet and sf}{

When \code{format = "geoparquet"} and the \code{sf} package is installed, the
returned object is an \code{sf} data frame with the CRS read from the GeoParquet
metadata (typically OGC:CRS84 / WGS84).  If \code{sf} is not installed, or
\code{as_sf = FALSE}, a plain data frame is returned with the WKT geometry
preserved as a character column (extracted from the WKB binary by the
\code{sfarrow} package if available, else left as raw).  Install \code{sf} with
\code{install.packages("sf")}.
}

\examples{
\dontrun{
eolas_key("your_key")

# 3-million-row geospatial dataset — first call downloads GeoParquet from CDN;
# subsequent calls return in <1 s via sidecar check.
gdf <- eolas_get_local("nz_parcels")

# Non-geo tabular dataset
df <- eolas_get_local("nz_cpi")

# Explicit cache directory (overrides library config — highest priority)
df <- eolas_get_local("nz_cpi", cache_dir = "/data/eolas-cache")

# Force CSV format
df <- eolas_get_local("nz_cpi", format = "csv_gz")

# Keep plain data.frame even for geo datasets
df <- eolas_get_local("nz_parcels", as_sf = FALSE)

# Arrow table — zero-copy, no sf allocation; suitable for DuckDB / dplyr
tbl <- eolas_get_local("nz_parcels", as_arrow = TRUE)
}
}
\seealso{
\code{\link[=eolas_sync_bulk]{eolas_sync_bulk()}}, \code{eolas_library_set()}, \url{https://docs.eolas.fyi/bulk-downloads/}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/changelog.R
\name{eolas_sync_changes}
\alias{eolas_sync_changes}
\title{Incrementally sync a changelog-tier dataset via the /changes feed}
\usage{
eolas_sync_changes(
  name,
  path,
  format = "parquet",
  progress = NULL,
  force = FALSE,
  base_url = EOLAS_BASE_URL
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_building_outlines"}.}

\item{path}{Where to write the materialised Parquet file. The sidecar lives at
\code{paste0(path, ".eolas-meta.json")}.}

\item{format}{Only \code{"parquet"} is supported for changelog sync.}

\item{progress}{Forwarded to \code{\link[=eolas_sync_bulk]{eolas_sync_bulk()}} for the baseline download bar.}

\item{force}{When \code{TRUE}, discard the incremental watermark and re-baseline from
a full bulk snapshot.}

\item{base_url}{API base URL.}
}
\value{
A list with \code{status}, \code{sync_mode = "changelog"}, \code{previous_seq}, \code{current_seq},
\code{ops_applied}, \code{path}, \code{current_snapshot_id}.
}
\description{
The OUT half of CDC. On the first call (cold start) downloads the full baseline via
\code{\link[=eolas_sync_bulk]{eolas_sync_bulk()}} and anchors the watermark at the current feed head. On subsequent calls pages
only the new changes since the watermark and pk-merges them into the local file (atomic rewrite),
applying the dataset's \code{current_state_filter} (e.g. \code{is_current = true} for SCD2). A \code{410}
(watermark expired) self-heals by re-baselining. Mirrors the Python client's \code{sync_changes}.
}

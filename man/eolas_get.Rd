% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{eolas_get}
\alias{eolas_get}
\title{Fetch dataset rows}
\usage{
eolas_get(
  name,
  start = NULL,
  end = NULL,
  limit = NULL,
  as_sf = NULL,
  base_url = EOLAS_BASE_URL
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_cpi"}.}

\item{start}{ISO date lower bound, e.g. \code{"2020-01-01"}. Optional.}

\item{end}{ISO date upper bound, e.g. \code{"2024-12-31"}. Optional.}

\item{base_url}{Override the API base URL (useful for testing).}
}
\value{
A \code{eolas_dataset} data frame with \code{date} coerced to \code{Date}.
}
\description{
The generic workhorse — use \code{\link[=eolas_get_statsnz]{eolas_get_statsnz()}}, \code{\link[=eolas_get_oecd]{eolas_get_oecd()}} etc. for
source-tagged results and a nicer print output.
}
\examples{
\dontrun{
eolas_key("your_key")
df <- eolas_get("nz_cpi", start = "2020-01-01")
library(ggplot2)
ggplot(df, aes(date, value)) + geom_line()
}
}

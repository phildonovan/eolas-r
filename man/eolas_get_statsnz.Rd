% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{eolas_get_statsnz}
\alias{eolas_get_statsnz}
\title{Fetch a Stats NZ series}
\usage{
eolas_get_statsnz(
  name,
  start = NULL,
  end = NULL,
  limit = NULL,
  as_sf = NULL,
  as_arrow = FALSE,
  ...
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_cpi"}.}

\item{start}{ISO date lower bound, e.g. \code{"2020-01-01"}. Optional.}

\item{end}{ISO date upper bound, e.g. \code{"2024-12-31"}. Optional.}

\item{limit}{Max rows to return. Default \code{NULL} requests the full dataset
(server enforces a 50,000-row cap on Free/Starter plans; Pro is unlimited).
Pass an integer to request fewer rows.}

\item{as_sf}{Convert geospatial datasets to an \code{sf} object (CRS = WGS84).
\code{NULL} (default) auto-converts when the dataset has a \code{geometry_wkt}
column AND the \code{sf} package is installed. \code{TRUE} forces conversion (errors
if \code{sf} is missing). \code{FALSE} keeps the raw WKT string column.
Install with \code{install.packages("sf")}. Cannot be combined with
\code{as_arrow = TRUE}.}

\item{as_arrow}{When \code{TRUE}, return an \code{arrow::Table} instead of a
data frame or \code{sf} object (zero-copy, no geometry materialisation).
Cannot be combined with \code{as_sf = TRUE}.}

\item{progress}{Bulk download/read progress when a whole-dataset pull is
auto-routed to \code{\link[=eolas_get_local]{eolas_get_local()}}. \code{NULL} (default) shows progress in
interactive sessions. See \code{\link[=eolas_get]{eolas_get()}}.}

\item{force}{Re-download the bulk cache even when locally current. See
\code{\link[=eolas_get]{eolas_get()}} and \code{\link[=eolas_cache_clear]{eolas_cache_clear()}}.}
}
\value{
A \code{eolas_dataset} data frame, an \code{sf} object when geometry is present
and conversion is enabled, or an \code{arrow::Table} when \code{as_arrow = TRUE}.
}
\description{
A named wrapper over \code{\link[=eolas_get]{eolas_get()}} that tags the result with the
"Stats NZ" source label.
}
\examples{
\dontrun{
eolas_key("your_key")
df <- eolas_get_statsnz("bds_enterprises_business_type", start = "2015-01-01")
library(ggplot2)
ggplot(df, aes(date, value)) + geom_line()
}
}

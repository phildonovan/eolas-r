% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrations.R
\name{eolas_integration}
\alias{eolas_integration}
\title{Generate connector configs for third-party data-pipeline tools}
\usage{
eolas_integration(
  platform,
  datasets,
  output_dir = NULL,
  force = FALSE,
  base_url = EOLAS_BASE_URL
)
}
\arguments{
\item{platform}{One of \code{"meltano"}, \code{"fivetran"}, \code{"azure-data-factory"}.}

\item{datasets}{Character vector of dataset names to include in the config.}

\item{output_dir}{Optional directory path. When supplied, the generated
files are written there (creating the directory if needed) and the path
to each written file is included in the returned list. When \code{NULL} (the
default), files are returned in-memory only.}

\item{force}{When \code{output_dir} is set: overwrite existing files. Default
\code{FALSE} skips files that already exist on disk.}

\item{base_url}{Override the API base URL (useful for testing).}
}
\value{
A list with elements:
\itemize{
\item \code{platform} — the platform name as echoed by the server.
\item \code{files} — a named list of \code{filename = content} (always populated).
\item \code{written} — character vector of paths actually written
(only present when \code{output_dir} is set).
\item \code{skipped} — character vector of paths skipped because they
already existed and \code{force = FALSE}.
}
}
\description{
Calls the eolas Enterprise-only \verb{/v1/integrations/<platform>} endpoint and
returns the generated config files. Optionally writes them to disk.
}
\details{
Supported platforms:
\itemize{
\item \code{"meltano"} — \code{meltano.yml} using \code{tap-rest-api-msdk}, plus README
and \code{.env.example}. \verb{meltano install && meltano run tap-eolas target-jsonl} and you're loading.
\item \code{"fivetran"} — Connector Builder YAML for paste-into-dashboard import.
\item \code{"azure-data-factory"} — linked-service + per-dataset REST datasets
+ copy pipeline JSON; usable via \verb{az datafactory} CLI or ADF Studio.
}

This is an Enterprise-plan feature. Non-Enterprise keys receive a 403
from the server; the upgrade pointer flows through verbatim as the error
message. See \url{https://eolas.fyi/#pricing}.
}
\examples{
\dontrun{
eolas_key("your_enterprise_key")

# In-memory: inspect what the server would generate
result <- eolas_integration("meltano", c("nz_cpi", "nz_gdp"))
names(result$files)
cat(result$files$meltano.yml)

# Write straight to a directory ready for `meltano install`
eolas_integration(
  "meltano",
  c("nz_cpi", "nz_gdp"),
  output_dir = "./my-pipeline"
)
}
}

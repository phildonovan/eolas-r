% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync_manifest.R
\name{.eolas_read_manifest}
\alias{.eolas_read_manifest}
\title{Read \verb{_eolas-manifest.json} from a dataset directory}
\usage{
.eolas_read_manifest(library_dir, dataset)
}
\arguments{
\item{library_dir}{Path to the root library directory.}

\item{dataset}{Dataset name (sub-directory under \code{library_dir}).}
}
\value{
A named list (the parsed manifest) or \code{NULL} if absent.
}
\description{
Parses and validates the manifest JSON.  Returns \code{NULL} when the file does
not exist (indicating the dataset has not been synced yet).  Raises on
parse / validation errors so callers can distinguish "first sync" (\code{NULL})
from "corrupt manifest" (error).
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{eolas_download}
\alias{eolas_download}
\title{Download a dataset via the live API}
\usage{
eolas_download(
  name,
  path,
  format = "csv",
  start = NULL,
  end = NULL,
  limit = NULL,
  progress = NULL,
  base_url = EOLAS_BASE_URL
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_cpi"}.}

\item{path}{Where to write the file (required).}

\item{format}{\code{"csv"} (default), \code{"parquet"}, \code{"arrow"}, or \code{"json"}.}

\item{start}{ISO date lower bound. Optional.}

\item{end}{ISO date upper bound. Optional.}

\item{limit}{Max rows. \code{NULL} (default) requests the full dataset (subject
to plan caps).}

\item{progress}{Download progress bar control (\code{"download"} phase only).
\code{NULL} auto-detects in interactive sessions; suppressed by
\code{EOLAS_NO_PROGRESS=1}.}

\item{base_url}{Override the API base URL (useful for testing).}
}
\value{
Invisibly the normalised output path.
}
\description{
Streams \code{GET /v1/datasets/{name}/data} to a file. Works for \strong{all}
datasets, including OECD and other licence-restricted tables where bulk
export is unavailable (e.g. \code{"nz_cpi"}). For whole-dataset pulls on very
large or geospatial tables, prefer \code{\link[=eolas_download_bulk]{eolas_download_bulk()}} when bulk export
is permitted.
}
\examples{
\dontrun{
eolas_key("your_key")
eolas_download("nz_cpi", path = "nz_cpi.csv")
eolas_download("nz_cpi", path = "nz_cpi.parquet", format = "parquet")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{eolas_get_eeca}
\alias{eolas_get_eeca}
\title{Fetch an EECA dataset (NZ energy use, EV chargers, regional heat demand)}
\usage{
eolas_get_eeca(
  name,
  start = NULL,
  end = NULL,
  limit = NULL,
  as_sf = NULL,
  as_arrow = FALSE
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_cpi"}.}

\item{start}{ISO date lower bound, e.g. \code{"2020-01-01"}. Optional.}

\item{end}{ISO date upper bound, e.g. \code{"2024-12-31"}. Optional.}

\item{limit}{Max rows to return. Default \code{NULL} requests the full dataset
(server enforces a 50,000-row cap on Free/Starter plans; Pro is unlimited).
Pass an integer to request fewer rows.}

\item{as_sf}{Convert geospatial datasets to an \code{sf} object (CRS = WGS84).
\code{NULL} (default) auto-converts when the dataset has a \code{geometry_wkt}
column AND the \code{sf} package is installed. \code{TRUE} forces conversion (errors
if \code{sf} is missing). \code{FALSE} keeps the raw WKT string column.
Install with \code{install.packages("sf")}. Cannot be combined with
\code{as_arrow = TRUE}.}

\item{as_arrow}{When \code{TRUE}, return an \code{arrow::Table} instead of a
data frame or \code{sf} object (zero-copy, no geometry materialisation).
Cannot be combined with \code{as_sf = TRUE}.}
}
\value{
A \code{eolas_dataset} data frame, or an \code{sf} object when geometry is
present and conversion is enabled.
}
\description{
A named wrapper over \code{\link[=eolas_get]{eolas_get()}} for datasets from the Energy Efficiency
and Conservation Authority (EECA). Covers NZ energy end-use by sector and
fuel type, public and co-funded EV charger locations, quarterly EV
penetration metrics by region and territorial authority, and regional
industrial process heat demand.
}
\details{
EV charger streams (\code{eeca_ev_chargers_public}, \code{eeca_ev_chargers_cofunded})
carry point geometry and refresh quarterly.
\code{eeca_energy_end_use} is the annual Energy End Use Database (EEUD).
\code{eeca_regional_heat_demand} is an Aug 2024 snapshot.
Source: \url{https://www.eeca.govt.nz/insights/data-tools/}.
Licence: CC-BY 4.0 NZ (Crown).
}
\examples{
\dontrun{
eolas_key("your_key")
df  <- eolas_get_eeca("eeca_energy_end_use")         # NZ energy by sector x fuel x end-use x year
gdf <- eolas_get_eeca("eeca_ev_chargers_public")     # public EV charging network (Point geometry)
df  <- eolas_get_eeca("eeca_ev_metrics_district")    # EV penetration by territorial authority
df  <- eolas_get_eeca("eeca_regional_heat_demand")   # industrial process heat by region x sector
}
}

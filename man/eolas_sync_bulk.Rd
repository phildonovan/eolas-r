% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bulk.R
\name{eolas_sync_bulk}
\alias{eolas_sync_bulk}
\title{Incrementally sync a bulk dataset file}
\usage{
eolas_sync_bulk(
  name,
  path,
  format = "parquet",
  freshness = "auto",
  progress = NULL,
  force = FALSE,
  base_url = EOLAS_BASE_URL,
  ...
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_cpi"}.}

\item{path}{\strong{Required.} File path where the data should live.  The
sidecar is written at \code{paste0(path, ".eolas-meta.json")}.
Parent directories are created automatically.}

\item{format}{\code{"parquet"} (default), \code{"csv_gz"}, or \code{"geoparquet"}.}

\item{freshness}{\code{"auto"} (default), \code{"monthly"}, or \code{"current"}.}

\item{progress}{Control the download progress bar (\code{"download"} phase).
See \code{\link[=eolas_get_local]{eolas_get_local()}} for the full \code{progress} selector vocabulary.
When \code{status = "unchanged"} no download bar is shown; an informative
cached-file message is printed instead.}

\item{force}{When \code{TRUE}, skip the sidecar "unchanged" fast path and
re-download the bulk file even when the local snapshot id already matches
the server (useful after corruption or to verify a fresh CDN copy).}

\item{base_url}{Override the API base URL (useful for testing).}

\item{...}{Reserved; currently ignored.}
}
\value{
A named list with the same fields as Python's \code{SyncResult}:
\describe{
\item{\code{status}}{\code{"downloaded"}, \code{"updated"}, or \code{"unchanged"}}
\item{\code{previous_snapshot_id}}{Snapshot id from the sidecar, or \code{NA} if none}
\item{\code{current_snapshot_id}}{Snapshot id from the server}
\item{\code{path}}{Normalised path to the data file}
\item{\code{bytes_downloaded}}{Bytes written (0 when unchanged)}
}
}
\description{
Checks whether the locally-cached file is still current by issuing a
lightweight HEAD request and reading the \code{X-Snapshot-Version} response
header.  If the snapshot id matches the sidecar, the function returns
immediately with \code{status = "unchanged"} and no data I/O.  Otherwise it
downloads the new snapshot, replaces the local file \strong{atomically} (via a
temp file + \code{file.rename()}), and updates the sidecar.
}
\section{Sidecar}{

A JSON file \verb{<path>.eolas-meta.json} is written next to the data file.
It stores the snapshot id, download timestamp, format, and source URL
and is read on the next call to perform the no-op check cheaply.
}

\section{Atomic replacement}{

The new file is downloaded to \verb{<path>.eolas-tmp-<rand>} and then renamed
over the original with \code{file.rename()}.  On most POSIX systems this is an
atomic inode swap; on Windows it uses \code{MoveFileExW} with
\code{MOVEFILE_REPLACE_EXISTING}.  Readers with the file open will see either
the old or the new content, never a partial write.
}

\examples{
\dontrun{
eolas_key("your_key")

# First call: full download
r <- eolas_sync_bulk("nz_cpi", path = "nz_cpi.parquet")
r$status           # "downloaded"
r$bytes_downloaded # e.g. 2100000

# Second call (same snapshot): no network I/O on the data file
r <- eolas_sync_bulk("nz_cpi", path = "nz_cpi.parquet")
r$status           # "unchanged"
r$bytes_downloaded # 0

# Poll for updates in a long-running script
repeat {
  r <- eolas_sync_bulk("nz_cpi", path = "nz_cpi.parquet")
  if (r$status != "unchanged") message("Updated to snapshot ", r$current_snapshot_id)
  Sys.sleep(3600)
}
}
}
\seealso{
\code{\link{eolas_download_bulk}}, \url{https://docs.eolas.fyi/bulk-downloads/}
}

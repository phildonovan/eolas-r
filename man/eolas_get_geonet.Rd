% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{eolas_get_geonet}
\alias{eolas_get_geonet}
\title{Fetch a GeoNet dataset (NZ earthquakes, volcanic alert levels, strong-motion sensors)}
\usage{
eolas_get_geonet(
  name,
  start = NULL,
  end = NULL,
  limit = NULL,
  as_sf = NULL,
  as_arrow = FALSE,
  ...
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_cpi"}.}

\item{start}{ISO date lower bound, e.g. \code{"2020-01-01"}. Optional.}

\item{end}{ISO date upper bound, e.g. \code{"2024-12-31"}. Optional.}

\item{limit}{Max rows to return. Default \code{NULL} requests the full dataset
(server enforces a 50,000-row cap on Free/Starter plans; Pro is unlimited).
Pass an integer to request fewer rows.}

\item{as_sf}{Convert geospatial datasets to an \code{sf} object (CRS = WGS84).
\code{NULL} (default) auto-converts when the dataset has a \code{geometry_wkt}
column AND the \code{sf} package is installed. \code{TRUE} forces conversion (errors
if \code{sf} is missing). \code{FALSE} keeps the raw WKT string column.
Install with \code{install.packages("sf")}. Cannot be combined with
\code{as_arrow = TRUE}.}

\item{as_arrow}{When \code{TRUE}, return an \code{arrow::Table} instead of a
data frame or \code{sf} object (zero-copy, no geometry materialisation).
Cannot be combined with \code{as_sf = TRUE}.}
}
\value{
A \code{eolas_dataset} data frame, or an \code{sf} object when geometry is
present and conversion is enabled.
}
\description{
A named wrapper over \code{\link[=eolas_get]{eolas_get()}} for datasets sourced from GeoNet,
operated by Earth Sciences New Zealand (formerly GNS Science). Covers
recent NZ earthquake activity (MMI>=3), volcanic alert levels for 12
monitored volcanoes, and strong-motion sensor station locations.
}
\details{
The earthquake catalogue (\code{geonet_quakes_recent}) is a rolling window of
recent events, not a historical archive. Refreshed every 6 hours.
Source: \url{https://www.geonet.org.nz}.
Licence: CC-BY 3.0 NZ (Earth Sciences New Zealand, formerly GNS Science).
}
\examples{
\dontrun{
eolas_key("your_key")
df  <- eolas_get_geonet("geonet_quakes_recent")         # rolling ~100 recent MMI>=3 quakes
df  <- eolas_get_geonet("geonet_volcanic_alert_levels") # 12 monitored NZ volcanoes
gdf <- eolas_get_geonet("geonet_strong_motion_sensors") # 25 strong-motion stations
}
}

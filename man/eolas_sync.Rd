% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{eolas_sync}
\alias{eolas_sync}
\title{Sync a single dataset to a local library directory}
\usage{
eolas_sync(
  name,
  library_dir = NULL,
  progress = NULL,
  base_url = EOLAS_BASE_URL
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"doc_huts"} or \code{"nz_parcels"}.}

\item{library_dir}{Root directory of the local data library.  A
sub-directory named \verb{<name>} is created inside it.  Accepts \code{~}-prefixed
paths.  When \code{NULL} (default) the library is resolved via
\code{eolas_resolve_library_dir()}: \code{EOLAS_LIBRARY} env var →
\verb{~/.eolas/config.json} → \verb{~/.cache/eolas/}.}

\item{progress}{Control the download progress bar.  \code{NULL} (default)
auto-detects: shown in interactive sessions.  \code{TRUE} forces on; \code{FALSE}
forces off.  Also suppressed by \code{EOLAS_NO_PROGRESS=1}.}

\item{base_url}{Override the API base URL (useful for testing).}
}
\value{
An \code{eolas_sync_result} S3 object.
}
\description{
Downloads or incrementally updates a dataset on disk, following the same
decision logic as \code{client.sync()} in the Python \code{eolas-data} package.
Both clients share the \verb{_eolas-manifest.json} format, so a library synced
from Python can be read from R and vice versa.
}
\section{Decision logic}{

\enumerate{
\item Read the local manifest (\code{NULL} → first sync → full bulk download).
\item Fetch \code{GET /v1/datasets/{name}} to get \code{current_snapshot_id} and
\code{incremental_supported}.
\item If the local snapshot id matches the server → \code{status = "unchanged"}.
\item If \code{incremental_supported = FALSE} → full re-download.
\item Attempt \verb{GET /v1/datasets/\{name\}/data/incremental?since_snapshot=<id>}.
A 410 or 400 falls back to a full re-download.
A 200 with \code{X-Eolas-Row-Count: 0} returns \code{"unchanged"}.
A 200 with data saves a delta file → \code{status = "snapshot_delta"}.
}
}

\section{Result object}{

Returns an \code{eolas_sync_result} list with fields:
\describe{
\item{\code{status}}{\code{"unchanged"}, \code{"snapshot_full"}, \code{"snapshot_delta"}, or \code{"error"}}
\item{\code{dataset}}{The dataset name passed in.}
\item{\code{library_dir}}{Resolved library directory.}
\item{\code{bytes_downloaded}}{Total bytes written to disk (0 for unchanged).}
\item{\code{rows_added}}{New rows added in this sync (0 for unchanged).}
\item{\code{files_added}}{New parquet files written (0 for unchanged).}
\item{\code{error}}{\code{NULL} for non-error statuses; error message string for \code{"error"}.}
}
}

\examples{
\dontrun{
eolas_key("your_key")

# First call: full bulk download
r <- eolas_sync("doc_huts", library_dir = "/data/eolas")
print(r)
# <eolas_sync_result dataset=doc_huts status=snapshot_full rows_added=2500 ...>

# Second call: unchanged
r2 <- eolas_sync("doc_huts", library_dir = "/data/eolas")
print(r2)
# <eolas_sync_result dataset=doc_huts status=unchanged rows_added=0 ...>
}
}

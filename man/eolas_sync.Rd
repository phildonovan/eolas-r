% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{eolas_sync}
\alias{eolas_sync}
\title{Sync a dataset to a local file, auto-routing on its CDC serving tier}
\usage{
eolas_sync(
  name,
  path,
  format = "parquet",
  progress = NULL,
  force = FALSE,
  base_url = EOLAS_BASE_URL
)
}
\arguments{
\item{name}{Dataset identifier, e.g. \code{"nz_building_outlines"}.}

\item{path}{Local file to materialise.}

\item{format}{Output format. Changelog sync requires \code{"parquet"}; bulk also accepts \code{"csv_gz"} /
\code{"geoparquet"}.}

\item{progress}{Tri-state progress bar control forwarded to the underlying sync.}

\item{force}{When \code{TRUE}, bypass local "unchanged" cache and re-sync from the
server. On changelog-tier datasets this re-baselines from a full bulk snapshot.}

\item{base_url}{API base URL.}
}
\value{
The result list from the dispatched sync (see \code{\link[=eolas_sync_changes]{eolas_sync_changes()}} / \code{\link[=eolas_sync_bulk]{eolas_sync_bulk()}}).
}
\description{
Reads \code{cdc_serving_tier} from the dataset metadata and dispatches:
\itemize{
\item \code{"changelog"} → \code{\link[=eolas_sync_changes]{eolas_sync_changes()}} — incremental /changes feed, pk-merged into the local file
(first call downloads a baseline; later calls apply only what changed).
\item anything else (\code{"snapshot"}) → \code{\link[=eolas_sync_bulk]{eolas_sync_bulk()}} — full-snapshot download, refreshed when the
server snapshot changes.
}
}
\details{
Both paths keep a \code{paste0(path, ".eolas-meta.json")} sidecar and return a list with at least
\code{status}, \code{path}, and \code{current_snapshot_id}; the changelog path additionally returns \code{sync_mode},
\code{previous_seq}, \code{current_seq}, and \code{ops_applied}.
}
\examples{
\dontrun{
# Same call works whether the dataset is snapshot- or changelog-tier:
eolas_sync("nz_building_outlines", path = "buildings.parquet")
}
}
